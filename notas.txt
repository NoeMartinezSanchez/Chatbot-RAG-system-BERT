#Para arrancar el ambiente virtual
chat_env\Scripts\activate 

#Para verificar si la api esta corriendo
netstat -ano | findstr :8000

# Ejecutar la ap
python -m api.main

# Abre en navegador:
# - http://localhost:8000       ← Interfaz web del chatbot

# Aca esta la aplicacion
http://localhost:8000/

# interfaz interactiva
http://localhost:8000/api/docs

# Prueba Intents

{
  "message": "hola, tengo una duda sobre el módulo",
  "user_id": "usuario_prueba"
}

# Prueba RAG

{
  "message": "No guarde mi número de folio proporcionado al finalizar su pre-registro",
  "user_id": "string",
  "conversation_id": "string"
}

# Limpiar cache del pip
pip cache purge


#Para cargar datos al sistema RAG
python scripts/upload_documents.py --file data/documents/tickets_2.xlsx



## Borrado manual de la base de conocimientos

# 1. Detén cualquier servidor/proceso del chatbot que esté corriendo
#    Esto libera los archivos para poder borrarlos

# 2. Navega al directorio de tu proyecto
cd /ruta/a/tu/proyecto

# 3. Borra TODOS los archivos de FAISS y documentos
rm -rf data/vector_store/*


Ahora deberías tener:

🌐 Interfaz Web: http://localhost:8000

📚 API Docs (Swagger): http://localhost:8000/docs

📖 API Redoc: http://localhost:8000/redoc

🩺 Health Check: http://localhost:8000/health

📊 Estadísticas: http://localhost:8000/stats

## COMO SE USA EL DEBUGER

# Prueba simple
python debug_rag_avanzado.py --query "¿Cómo cambio mi correo electrónico?"

# Suite completa
python debug_rag_avanzado.py --suite

# Con nombre personalizado
python debug_rag_avanzado.py --suite --name "antes_mejoras"

# Comparar después de mejoras
python debug_rag_avanzado.py --suite --name "despues_mejoras" --compare "antes_mejoras"